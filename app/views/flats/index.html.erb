<body>
  <div class="banner">
    <h1> Index de mes flats</h1>
  </div>

  <div class="container">
    <% @flats.each do |flat| %>
    <div class="card">
    <%= link_to flat_path(flat) do %>
      <ul class="card-list">
        <li class="card-list-name"><%= flat.name   %></li>
        <li class="card-list-address"><%= flat.address  %></li>
        <li class="card-list-description">Address: <%=  flat.description %></li>
        <li class="card-list-price"><%=  flat.price_per_night %>€</li>
      </ul>
     <% end %>
    </div>
    <% end %>
  </div>
  <%= link_to 'Add your amazing Flat!', new_flat_path, class:"btn-edit" %>
  <%= link_to 'Add your amazing Flat!', new_flat_path, class:"btn-edit-2" %>
  <%= link_to 'Add your amazing Flat!', new_flat_path, class:"btn-edit-3" %>
  <%= link_to 'Add your amazing Flat!', new_flat_path, class:"btn-edit-4" %>
</body>
